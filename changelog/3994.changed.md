Derive graph-conditional usage from platform support: on devices without CUDA graph conditional nodes, the MuJoCo Warp integration and the Kamino PADMM solver now select their documented non-conditional paths instead of failing at capture, and the implicit-MPM examples skip graph capture. No behavior change where conditional nodes are supported; no migration needed.
